server.port=8080

server.servlet.session.timeout=-1
# Полный URL POST для синхронизации позиций (планировщик). Пусто — запрос не выполняется (позиции из bs.json при старте).
url=
access=

spring.datasource.url=
spring.datasource.username=
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# spring.jackson.serialization.write-dates-as-timestamps=false

scheduler.cron=0 0 3 * * *
scheduler.zone=Asia/Yekaterinburg
app.runOnStartup=true
output.path=src/main/resources/data_base_station.json

# JWT Settings
jwt.secret=
jwt.expiration=


